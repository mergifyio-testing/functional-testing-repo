pull_request_rules:
- actions:
    merge:
      method: rebase
  conditions:
  - base=20210323065807/test_short_teams/master
  - status-success=continuous-integration/fake-ci
  - approved-reviews-by=@testing
  name: Merge on master
