pull_request_rules:
- actions:
    merge:
      strict: true
      strict_method: rebase
  conditions:
  - base=20210116180049/test_merge_strict_rebase/master
  - status-success=continuous-integration/fake-ci
  - '#approved-reviews-by>=1'
  name: smart strict merge on master
