pull_request_rules:
- actions:
    merge:
      priority: high
      strict: smart+ordered
  conditions:
  - base=20200625150351/test_merge_priority/master
  - label=high
  name: Merge priority high
- actions:
    merge:
      strict: smart+ordered
  conditions:
  - base=20200625150351/test_merge_priority/master
  - label=medium
  name: Merge priority default
- actions:
    merge:
      priority: 1
      strict: smart+ordered
  conditions:
  - base=20200625150351/test_merge_priority/master
  - label=low
  name: Merge priority low
