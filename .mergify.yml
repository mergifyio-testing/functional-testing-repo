pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20210819083542/test_queue_cancel_and_refresh/master
  - label=queue
  name: Tchou tchou
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
