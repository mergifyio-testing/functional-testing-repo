pull_request_rules:
- actions:
    merge:
      strict: smart
      strict_smart_behavior: always-append
      strict_smart_priority_labels:
      - urgent
      - medium
  conditions:
  - base=20200523210905/test_merge_priority/master
  - label=ready
  name: Merge on master
