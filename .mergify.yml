pull_request_rules:
- actions:
    merge:
      method: merge
  conditions:
  - base=20200911203924/test_merge_and_closes_issues/master
  - status-success=continuous-integration/fake-ci
  name: Merge on master
