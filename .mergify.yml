pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210819140616/test_queue_no_inplace/master
  - label=queue
  name: Merge priority high
queue_rules:
- allow_speculative_check_inplace: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
