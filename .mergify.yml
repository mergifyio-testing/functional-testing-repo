pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210105161638/test_queue_cant_create_tmp_pull_request/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
