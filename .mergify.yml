pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210130180339/test_ongoing_train_second_pr_ready_first/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
