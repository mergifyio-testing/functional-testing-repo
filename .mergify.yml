pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210104101043/test_ongoing_train/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
