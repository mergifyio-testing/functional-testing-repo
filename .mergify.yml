pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210817100157/test_batch_split_queue/master
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
