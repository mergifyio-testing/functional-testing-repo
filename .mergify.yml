pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210621091056/test_ongoing_train_basic/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
