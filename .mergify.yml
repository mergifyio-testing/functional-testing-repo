pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210820184334/test_queue_no_inplace/master
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_speculative_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
