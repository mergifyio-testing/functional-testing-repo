pull_request_rules:
- actions:
    merge:
      strict: smart
  conditions:
  - base=20200503075431/test_merge_failure_smart_strict/master
  - status-success=continuous-integration/fake-ci
  name: strict merge on master
