pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210203133801/test_queue_manual_merge/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
