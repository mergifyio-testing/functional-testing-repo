pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210820091125/test_queue_with_ci_in_pull_request_rules/master
  - label=queue
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
