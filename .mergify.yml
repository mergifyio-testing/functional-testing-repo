pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210715075117/test_queue_already_ready/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
