pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210621151148/test_queue_with_labels/master
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - label=foobar
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
