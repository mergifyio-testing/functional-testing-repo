pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20210130192917/test_queue_no_tmp_pull_request/master
  - label=queue
  name: Merge train
queue_rules:
- conditions:
  - status-success=continuous-integration/ci
  name: default
